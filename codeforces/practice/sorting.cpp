#include <bits/stdc++.h>
using namespace std;

struct P{
    int x, y;
}

int main(){
    vector<int> v = {2, 3, 4, 5, 6};
    v.push_back(5);
    sort(v.begin(), v.end());
    
    int a[] = {2, 3, 5, 6, 7};
    sort(a, a + 5);
}
