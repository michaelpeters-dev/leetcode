import java.io.*;
import java.util.*;

// CSES requires the class to be public and named Main.
// Locally, javac needs the file named Main.java to match — so keep each
// Java problem in its OWN sub-folder (e.g. cses/1068/Main.java).
public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();

        // Example:
        //   int n = Integer.parseInt(br.readLine().trim());
        //   StringTokenizer st = new StringTokenizer(br.readLine());

        System.out.print(sb);
    }
}
