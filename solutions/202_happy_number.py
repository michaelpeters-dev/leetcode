# Problem: Happy Number
# Number: 202
# Difficulty: Easy
# URL: https://leetcode.com/problems/happy-number/
# Submission Status: Accepted
# Runtime: 0 ms
# Memory: N/A

        while(n!=1):        while(n!=1):
                bad_set //= 10                bad_set //= 10
            bad_set = total            bad_set = total
            temp.add(bad_set)            temp.add(bad_set)

            if n in temp:            if n in temp:
                return False                return False
                total += lsd ** 2                total += lsd ** 2
                lsd = bad_set%10                lsd = bad_set%10
            while bad_set!=0:            while bad_set!=0:
            total = 0            total = 0
        for i in range(1, 1000):        for i in range(1, 1000):
        bad_set = 2        bad_set = 2
        temp.add(2)        temp.add(2)
        temp = set()        temp = set()
    def isHappy(self, n: int) -> bool:    def isHappy(self, n: int) -> bool:
class Solution:class Solution:
